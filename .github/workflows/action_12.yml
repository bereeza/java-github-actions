name: Using reusable workflow
on: workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Reusing reusable workflow
        uses: bereeza/java-github-actions/.github/workflows/action_1.yml@main
